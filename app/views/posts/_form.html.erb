<%= form_for(@post) do |f| %>
  <% if @post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
<div class="field">
  <%= f.label :add_photo, :class => "form-control" %>
              <%= f.file_field :postcover, :autofocus => true, :placeholder => 'Add image', :class => "form-control" %> 
              </div>

 
<div class="field"> 

<%= f.collection_select :postcategory_id, Postcategory.all, :id, :name, {promt: "Choose a category" }, input_html: { class: "dropdown-toggle" } %>
 </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>

<% end %>


